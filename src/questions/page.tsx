import React from "react";
import CustomHeader from "../components/CustomHeader";

const CTFs = () => {
  return (
    <div className="w-screen h-screen">
      <CustomHeader />
      <div className="font-neuebit">CTFs</div>
    </div>
  );
};

export default CTFs;
